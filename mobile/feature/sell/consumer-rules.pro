# This module contributes no consumer ProGuard rules.
# The file exists because build.gradle.kts declares it: a consumerProguardFiles entry
# pointing at a missing file is indistinguishable from one pointing at rules that work.
# App-wide rules live in app/proguard-rules.pro.
