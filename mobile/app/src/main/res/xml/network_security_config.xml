<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production/release: HTTPS only. Debug build type sets @bool/cleartext_permitted via Gradle. -->
    <base-config cleartextTrafficPermitted="@bool/cleartext_permitted">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds only: trust user-installed certificates (e.g. Charles, mitmproxy) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
