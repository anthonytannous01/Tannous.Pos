# This file ensures the ci/ directory is tracked in Git
# The CI pipeline will create the following files here:
# - keystore.jks (from base64 secret)
# - play-service-account.json (from secret)
# - firebase-service-account.json (from secret)
# - release-notes.txt (generated from Git commits)
# - version-info.txt (generated by versioning script)
