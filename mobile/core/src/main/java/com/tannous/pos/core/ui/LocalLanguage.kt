package com.tannous.pos.core.ui

import androidx.compose.runtime.compositionLocalOf

/** True when the active UI language is Arabic. Read anywhere in the Compose tree. */
val LocalIsArabic = compositionLocalOf { false }
